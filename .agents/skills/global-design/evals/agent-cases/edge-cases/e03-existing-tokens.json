{
  "id": "e03-existing-tokens",
  "category": "edge-cases",
  "query": "У нас уже есть дизайн-система на Tailwind с готовой палитрой в hex. Нужен новый экран настроек. Что делать с цветом?",
  "expected_behavior": [
    "Works inside the existing system instead of replacing it unasked",
    "Does not demand a migration as the price of the screen",
    "Explains the trade-off if it recommends OKLCH"
  ],
  "behavioral_checks": {
    "output_contract": [
      "(существующ|текущ|уже есть|existing)",
      "(токен|token|переменн|var\\()"
    ]
  }
}
